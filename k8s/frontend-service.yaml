apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: blogging-frontend
spec:
  type: NodePort
  selector:
    app: blogging-frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30000
